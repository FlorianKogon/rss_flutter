import 'package:flutter/material.dart';
import 'textflo.dart';

class Loading extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: TextFlo("Loading in progress.."),
    );
  }
}